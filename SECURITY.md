# 安全政策

## 支持范围

当前仅维护最新版本。项目是本地工具，默认只监听 `127.0.0.1`；不要把服务端口直接暴露到局域网或公网。

## 报告漏洞

请优先通过 GitHub 仓库的 **Security → Report a vulnerability** 私下报告，并附上影响、复现步骤和建议修复方式。不要在公开 Issue 中披露尚未修复的密钥或可直接利用的细节。

如果仓库尚未开启私有漏洞报告，请通过维护者 [@cattreemaybe](https://github.com/cattreemaybe) 主页提供的联系方式联系。维护者会尽量在 7 天内确认收到报告，但不承诺固定修复时限。

## 安全边界

- `.env` 中的令牌只在后端读取，不应提交到 Git。
- 模型模式会把图片数据发送到用户自行配置的兼容服务。
- 为避免服务端请求伪造（SSRF），程序不下载图片服务返回的任意 URL，只接受 Base64 图片结果。
- 上传大小和像素数有限制，但本项目不是面向不可信公网流量设计的多用户服务。
